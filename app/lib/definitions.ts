export type Match = {
    id: string;
    team: string;
    goals: number;
    assists: string;
    goalDifference: number;
    duration: number;
    date: string;
};

export type Team = {
    id: string;
    name: string;
}